# Trial evidence

The solution writes annotated live camera JPGs, location JSON sidecars and trial
JSONL logs here. Each JPG contains simulation and UTC timestamps. Docker maps
this folder to `/opt/erc_ws/erc_images`. No example or manually edited scoring
images are included. Generated files are ignored by Git to avoid accidentally
mixing evidence from different trials; preserve the files from the final video.
